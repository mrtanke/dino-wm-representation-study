# Course Project Scripts

Target location for project-owned automation that should not live in the
upstream-shaped root script layer unless required for compatibility.

Examples:

- closeout helpers
- export parsers
- paper figure helpers
- repo sync helpers
